<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Camera capture path: the web <input capture> launches ACTION_IMAGE_CAPTURE.
         Android requires CAMERA to be GRANTED at runtime once it is declared here,
         so the app requests it before opening the camera (src/native/camera.ts) and
         degrades gracefully (file/gallery/PDF still work) if denied. -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- No READ_MEDIA_IMAGES / READ_EXTERNAL_STORAGE: every media entry is a
         user-initiated pick through the WebView file chooser (system Photo Picker
         for images, SAF for PDFs), which hands back a permissionless content:// URI
         grant — the app never reads the media library. Broad storage permissions
         are therefore unused and were rejected under Google Play's Photo & Video
         Permissions policy (see docs/PHOTO_VIDEO_PERMISSIONS_REJECTION_RECON_2026-07-23.md). -->

    <!-- Camera is optional: file/gallery/PDF upload works without it, so don't
         let Play Store filter out camera-less devices. -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
</manifest>
